## Editor

**Usage**

```typescript
import Editor from "./editor";

const code = "function hello() {\n\talert('Hello world!!!');\n}";
return (
  <div style={{ height: "100px", width: "500px" }}>
    <Editor code={code} />
  </div>
);
```
