import React from "react";
import { UILayout } from "./layout";

const App: React.FC<{}> = () => {
  return (
    <div style={{ height: "100%", width: "100%" }}>
      <UILayout />
    </div>
  );
};

export default App;
